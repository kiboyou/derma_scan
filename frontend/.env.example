# Frontend environment variables for API URLs
# Base path used by the client (browser) to call the API through Next.js proxy or direct server
NEXT_PUBLIC_API_BASE=/api

# Backend server URL for Next.js rewrites (dev/proxy). Example: http://localhost:8000 or http://api:8000 in Docker
API_SERVER=http://localhost:8000
